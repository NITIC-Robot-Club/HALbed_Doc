{
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "scripts": {
    "docs:dev": "node tools/generate-content-catalog.mjs && vitepress dev",
    "docs:build": "node tools/generate-content-catalog.mjs && vitepress build",
    "docs:preview": "vitepress preview"
  }
}
