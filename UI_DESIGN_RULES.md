# HALbed UI Design Rules

この文書は、HALbed Docs のUIを作成・改修するときの判断基準です。見た目の新しさより、読者が目的の情報へ迷わず到達できることを優先します。

## 1. 基本方針

- 目的を1文で言える画面だけを作る。目的が複数ある場合はページを分ける。
- 画面には主役を1つだけ置く。検索、記事一覧、説明、CTAを同じ強さで競わせない。
- 情報は装飾より前に置く。装飾を削っても意味が失われないなら、装飾は追加しない。
- 既存のWeb・OSパターンを優先する。検索は検索欄、階層はリスト、移動はリンクとして表現する。
- Apple HIGの「明快さ・簡潔さ・一貫性・可読性」をWeb向けに適用する。

## 2. AIっぽさを避けるルール

次の表現は原則使わない。使う場合は、情報理解を助ける具体的な理由をレビューに残す。

- 暗い巨大ヒーローに白い大見出しを載せる構成
- グラデーション文字、虹色のカテゴリ、意味のない発光や円形の装飾
- すべてを角丸カード・ピル・バッジにすること
- ガラス風の半透明背景、過剰なぼかし、強いドロップシャドウ
- 「未来的」「スマート」「次世代」など意味が曖昧なコピー
- 小さな英語ラベルを大量に添えて、画面をプロダクト風に演出すること
- クリックできるものを、ボタン・カード・チップ・リンクで重複表現すること

AIっぽさの判定は、次の質問で行う。

1. 色・影・角丸を消しても、情報の優先順位が理解できるか。
2. その装飾を外した方が記事タイトルや検索欄が読みやすくならないか。
3. 画面にある要素を、同じ目的のOS標準UIで置き換えられないか。
4. 初見の人が「何を押せばよいか」ではなく「何を読めばよいか」を理解できるか。

## 3. 情報設計

- トップページは、主検索、主要カテゴリ、次の行動の3層までにする。
- 全件一覧はトップに埋め込まない。記事数が10件を超える場合は一覧ページへ分離する。
- トップの初期表示に記事カードを大量に置かない。おすすめは最大6件、基本はカテゴリと一覧への導線を置く。
- 同じ記事へ到達する導線を3種類以上作らない。
- カテゴリはカードより、番号付きのリスト行を優先する。タイトル、説明、件数、移動先を同じ行で読めるようにする。
- 記事一覧は本文の抜粋、更新日、タグなど「選ぶための情報」を最低1つ表示する。

## 4. 見た目と操作

- アクセントカラーは1色を基本とし、状態表示以外で色を増やさない。
- 背景は無彩色を基本にする。セクションごとに色を変えない。
- 角丸は小さく一貫させる。ピル形はタグや短い状態表示に限定する。
- 影は階層が必要な浮遊要素だけに使う。平面の一覧には影を使わない。
- 見出しは3段階以内。英語のeyebrowは、意味を補う場合だけ使う。
- 本文は読みやすさを優先し、細い文字・小さすぎる文字・長い行を避ける。
- クリック範囲、フォーカス表示、キーボード操作、モバイル幅を必ず確認する。
- 検索には対象範囲を明示し、Enterで結果ページへ移動できるようにする。

## 5. 評価表

各項目を10点満点で採点し、合計平均9.5以上を合格とする。スクリーンショットだけでなく、DOM計測と実操作で確認する。

| 評価項目 | 合格条件 |
| --- | --- |
| 探索性 | 初期画面から検索・カテゴリ・全件一覧のどれかを迷わず選べる |
| 視認性 | 主要見出し、検索欄、次の行動が1画面で判別できる |
| 情報階層 | 主役が1つで、補助要素が主役と競合しない |
| 走査性 | 記事やカテゴリを上から下へ短時間で比較できる |
| 操作性 | 検索、リンク、キーボード、モバイル操作が実際に動く |
| 一貫性 | 色、余白、リンク、リスト表現がページ間で揃っている |
| AIっぽさの低さ | 不要なグラデーション、カード、ピル、英語演出、装飾がない |
| 可読性 | 文字サイズ、行長、コントラスト、説明文が十分である |
| 密度 | 初期画面が空疎すぎず、情報過多でもない |
| レスポンシブ | 390px幅で横溢れや過度な縦長化がない |

## 6. 実装前後のチェック

- [ ] 画面の主目的を1文で書いた
- [ ] 主要アクションを1つに絞った
- [ ] 装飾を外しても情報階層が成立する
- [ ] 初期表示の要素数とページ高さを計測した
- [ ] 390px幅とデスクトップ幅でスクリーンショットを確認した
- [ ] キーボード操作と検索結果の引き渡しを確認した
- [ ] 上表を採点し、AIっぽさの具体的な要因を記録した

## 7. 参考にした設計方針

- [Apple Human Interface Guidelines — Design principles](https://developer.apple.com/design/human-interface-guidelines/design-principles)
  - 目的、柔軟性、簡潔さ、クラフト、自然な楽しさを判断軸にする。
- [Apple Human Interface Guidelines — Searching](https://developer.apple.com/design/human-interface-guidelines/searching)
  - 検索が重要なら主要な位置に置き、検索範囲を明示し、入口を増やしすぎない。
- [Apple Human Interface Guidelines — Lists and tables](https://developer.apple.com/design/human-interface-guidelines/lists-and-tables)
  - 文章を比較する画面では、カードの集合より走査しやすいリストを優先する。
- [Apple Human Interface Guidelines — Typography](https://developer.apple.com/design/human-interface-guidelines/typography)
  - 文字の可読性と階層を優先し、細い文字や過度な文字装飾を避ける。
- [Apple Human Interface Guidelines — Color](https://developer.apple.com/design/human-interface-guidelines/color)
  - 色の意味を一貫させ、装飾のためだけに色を増やさない。
